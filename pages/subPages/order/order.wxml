<!--pages/subPages/order/order.wxml-->
<view>
  <view class="tab-box">
    <van-tabs color="#447fff" sticky swipeable animated active="{{ activeTab }}" bind:change="tabChange">
      <van-tab wx:key="index" wx:for="{{tabs}}" title="{{item.title}}">
        <view class="content-box">
          <view class="card-item" wx:for="{{list}}" wx:key="index">
            <van-cell-group inset>
              <van-cell title="{{item.merchantName}}">
                <text class="waitpay">{{item.payStatus}}</text>
              </van-cell>
              <van-cell border="{{false}}" title="{{itemm.plateNo}}">
                <text class="waitpay">¥{{item.totalAmount}}</text>
              </van-cell>
              <van-cell border="{{false}}" title="{{item.remark}}" value="创建时间: {{item.createdTime}}"/>
              <van-cell>
                <van-button size="small" plain type="info">去支付</van-button>
              </van-cell>
            </van-cell-group>
          </view>
          <view class="has-more" wx:if="{{list.length > 0}}">{{hasMore ? '上拉加载更多' : '~到底啦~'}}</view>
          <view wx:else>
            <nodata>暂无数据</nodata>
          </view>
        </view>
      </van-tab>
    </van-tabs>
  </view>
</view>
