<view class="tag-group-box">
  <view style="padding-right:10rpx;" wx:for="{{orgTags}}" wx:key="item">
    <van-tag class="fs-22" plain type="primary">{{translator[item] || item || '-'}}</van-tag>
  </view>
  <van-tag class="fs-22" wx:if="{{showTimer}}" plain>24h</van-tag>
</view>
