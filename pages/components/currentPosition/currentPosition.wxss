/* pages/components/currentPosition/currentPosition.wxss */
.pos-info {
  display: flex;
  align-items: center;
}
.address-name {
  padding-left: 8rpx;
  overflow:hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
  -o-text-overflow:ellipsis;
  word-break: break-all;
}